"use client";
import {FormEvent,useEffect,useState} from "react";import {supabase} from "../../lib/supabase";
type Service={id:string;name:string;description:string|null;pricing_unit:"kg"|"piece"|"load"|"package";default_price:number;loyalty_points:number;active:boolean};type Audit={id:string;action:string;entity_name:string;details:any;performed_by_name:string;performed_by_email:string;created_at:string};const peso=new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP"});
export default function ServicesPage(){const [services,setServices]=useState<Service[]>([]),[audit,setAudit]=useState<Audit[]>([]),[message,setMessage]=useState(""),[showNew,setShowNew]=useState(false),[form,setForm]=useState({name:"",description:"",pricing_unit:"kg",default_price:"",loyalty_points:"0"});useEffect(()=>{void load()},[]);async function load(){const [s,a]=await Promise.all([supabase.from("services").select("id,name,description,pricing_unit,default_price,loyalty_points,active").order("active",{ascending:false}).order("name"),supabase.rpc("get_management_audit_log",{p_category:"service",p_limit:50})]);if(s.error)setMessage(s.error.message);else setServices((s.data??[]).map((x:any)=>({...x,default_price:Number(x.default_price),loyalty_points:Number(x.loyalty_points??0)})));if(!a.error)setAudit(a.data??[])}async function createService(e:FormEvent){e.preventDefault();const {error}=await supabase.rpc("create_laundry_service",{p_name:form.name,p_description:form.description||null,p_pricing_unit:form.pricing_unit,p_default_price:Number(form.default_price),p_loyalty_points:Number(form.loyalty_points)||0});if(error){setMessage(error.message);return}setForm({name:"",description:"",pricing_unit:"kg",default_price:"",loyalty_points:"0"});setShowNew(false);setMessage("Service created and logged.");await load()}return <main className="workspace"><header><div><p className="eyebrow">MANAGEMENT</p><h1>Services & Pricing</h1><p className="muted">Manage laundry services, pricing units, rates and reward points. Changes are recorded in the audit history.</p></div><div className="headerActions"><button className="primary" onClick={()=>setShowNew(v=>!v)}>+ New Service</button></div></header>{message&&<p className="notice">{message}</p>}{showNew&&<section className="panel" style={{marginBottom:18}}><form className="gridForm" onSubmit={createService}><label>Service name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Pricing unit<select value={form.pricing_unit} onChange={e=>setForm({...form,pricing_unit:e.target.value})}><option value="kg">Per kg</option><option value="piece">Per piece</option><option value="load">Per load</option><option value="package">Package</option></select></label><label>Price<input type="number" min="0" step="0.01" value={form.default_price} onChange={e=>setForm({...form,default_price:e.target.value})} required/></label><label>Reward points<input type="number" min="0" step="1" value={form.loyalty_points} onChange={e=>setForm({...form,loyalty_points:e.target.value})}/></label><label className="wide">Description<input value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><button className="primary wide">Create Service</button></form></section>}<section className="panel"><div className="panelHead"><div><h2>Service Catalog</h2><span>{services.filter(s=>s.active).length} active services</span></div></div><div className="customerTableWrap"><table className="customerTable"><thead><tr><th>Service</th><th>Pricing Unit</th><th>Price</th><th>Reward Points</th><th>Status</th><th></th></tr></thead><tbody>{services.map(s=><tr key={s.id}><td><strong>{s.name}</strong><br/><small>{s.description||"—"}</small></td><td>{s.pricing_unit}</td><td><strong>{peso.format(s.default_price)}</strong></td><td>{s.loyalty_points.toLocaleString()} pts</td><td>{s.active?"Active":"Inactive"}</td><td><button className="secondary" onClick={()=>location.href=`/services/edit?id=${s.id}`}>Edit</button></td></tr>)}</tbody></table></div></section><section className="panel" style={{marginTop:18}}><div className="panelHead"><div><h2>Adjustment History</h2><span>Latest service and pricing changes</span></div></div><div className="customerTableWrap"><table className="customerTable"><thead><tr><th>Date & Time</th><th>Service</th><th>Action</th><th>Changed By</th><th>Change</th></tr></thead><tbody>{audit.map(a=><tr key={a.id}><td>{new Date(a.created_at).toLocaleString("en-PH")}</td><td><strong>{a.entity_name||"Service"}</strong></td><td>{a.action}</td><td><strong>{a.performed_by_name}</strong><br/><small>{a.performed_by_email}</small></td><td>{a.action==="created"?`Created at ${peso.format(Number(a.details?.price??0))}`:`${peso.format(Number(a.details?.before?.price??0))} → ${peso.format(Number(a.details?.after?.price??0))}; points ${a.details?.before?.loyalty_points??0} → ${a.details?.after?.loyalty_points??0}`}</td></tr>)}</tbody></table>{!audit.length&&<p className="empty">No service adjustments logged yet.</p>}</div></section></main>}
