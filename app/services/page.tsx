"use client";
import {FormEvent,useEffect,useState} from "react";
import {supabase} from "../../lib/supabase";
type Service={id:string;name:string;description:string|null;pricing_unit:"kg"|"piece"|"load"|"package";default_price:number;loyalty_points:number;active:boolean};
const peso=new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP"});
export default function ServicesPage(){const [services,setServices]=useState<Service[]>([]),[message,setMessage]=useState(""),[showNew,setShowNew]=useState(false),[form,setForm]=useState({name:"",description:"",pricing_unit:"kg",default_price:"",loyalty_points:"0"});useEffect(()=>{void load()},[]);async function load(){const {data,error}=await supabase.from("services").select("id,name,description,pricing_unit,default_price,loyalty_points,active").order("active",{ascending:false}).order("name");if(error)setMessage(error.message);else setServices((data??[]).map((x:any)=>({...x,default_price:Number(x.default_price),loyalty_points:Number(x.loyalty_points??0)})))}async function createService(e:FormEvent){e.preventDefault();const {error}=await supabase.rpc("create_laundry_service",{p_name:form.name,p_description:form.description||null,p_pricing_unit:form.pricing_unit,p_default_price:Number(form.default_price),p_loyalty_points:Number(form.loyalty_points)||0});if(error){setMessage(error.message);return}setForm({name:"",description:"",pricing_unit:"kg",default_price:"",loyalty_points:"0"});setShowNew(false);setMessage("Service created.");await load()}return <main className="workspace"><header><div><p className="eyebrow">MANAGEMENT</p><h1>Services & Pricing</h1><p className="muted">Manage laundry services, pricing units, rates and reward points.</p></div><div className="headerActions"><button className="primary" onClick={()=>setShowNew(v=>!v)}>+ New Service</button></div></header>{message&&<p className="notice">{message}</p>}{showNew&&<section className="panel" style={{marginBottom:18}}><form className="gridForm" onSubmit={createService}><label>Service name<input value={form.name} onChange={e=>setForm({...form,name:e.target.value})} required/></label><label>Pricing unit<select value={form.pricing_unit} onChange={e=>setForm({...form,pricing_unit:e.target.value})}><option value="kg">Per kg</option><option value="piece">Per piece</option><option value="load">Per load</option><option value="package">Package</option></select></label><label>Price<input type="number" min="0" step="0.01" value={form.default_price} onChange={e=>setForm({...form,default_price:e.target.value})} required/></label><label>Reward points<input type="number" min="0" step="1" value={form.loyalty_points} onChange={e=>setForm({...form,loyalty_points:e.target.value})}/></label><label className="wide">Description<input value={form.description} onChange={e=>setForm({...form,description:e.target.value})}/></label><button className="primary wide">Create Service</button></form></section>}<section className="panel"><div className="panelHead"><div><h2>Service Catalog</h2><span>{services.filter(s=>s.active).length} active services</span></div></div><div className="customerTableWrap"><table className="customerTable"><thead><tr><th>Service</th><th>Pricing Unit</th><th>Price</th><th>Reward Points</th><th>Status</th><th></th></tr></thead><tbody>{services.map(s=><tr key={s.id}><td><strong>{s.name}</strong><br/><small>{s.description||"—"}</small></td><td>{s.pricing_unit}</td><td><strong>{peso.format(s.default_price)}</strong></td><td>{s.loyalty_points.toLocaleString()} pts</td><td>{s.active?"Active":"Inactive"}</td><td><button className="secondary" onClick={()=>location.href=`/services/edit?id=${s.id}`}>Edit</button></td></tr>)}</tbody></table>{!services.length&&<p className="empty">No services configured yet.</p>}</div></section></main>}
