"use client";

import {useEffect,useMemo,useState} from "react";
import {supabase} from "../../lib/supabase";
import {cacheReference,getReference,queueJob} from "../../lib/offlineQueue";

type DispatchJob={job_type:"pickup"|"delivery";status:string};
type Order={id:string;order_code:string;branch_id:string;customer_id:string|null;status:string;payment_status:string;subtotal:number;discount:number;total:number;amount_paid:number;created_at:string;delivery_addon_type?:"pickup"|"delivery"|"pickup_delivery"|null;delivery_distance_label?:string|null;delivery_fee?:number;delivery_jobs?:DispatchJob[];customers:{full_name:string;customer_code:string}|null};
type Access={settings?:Record<string,any>;permissions?:Record<string,boolean>};
type GCashAccount={integration_type:string;merchant_name:string|null;account_number:string|null;qr_image_url:string|null;instructions:string|null;active:boolean};
type QrPayment={transaction_id?:string;payment_intent_id?:string;amount:number;status:string;qr_image_data?:string|null;expires_at?:string|null;test_mode?:boolean;test_url?:string|null;loading?:boolean;error?:string|null};
const peso=new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP"});
const label=(s:string)=>s==="qrph"?"QR Ph":s.replaceAll("_"," ").replace(/\b\w/g,c=>c.toUpperCase());
const fallbackWorkflow=["received","sorting","washing","drying","folding","ready","completed"];
const fallbackPayments=["cash","gcash","maya","card","bank_transfer","qrph"];
const CACHE="orders-page-v1";

export default function OrdersPage(){
 const [orders,setOrders]=useState<Order[]>([]),[access,setAccess]=useState<Access>({}),[gcash,setGcash]=useState<GCashAccount|null>(null),[message,setMessage]=useState(""),[search,setSearch]=useState(""),[statusFilter,setStatusFilter]=useState("all");
 const [payment,setPayment]=useState<Record<string,{amount:string;method:string;reference:string}>>({});const [qrPayments,setQrPayments]=useState<Record<string,QrPayment>>({});
 useEffect(()=>{load();const sync=()=>setTimeout(()=>load(),500);window.addEventListener("online",sync);window.addEventListener("labaflow:offline-synced",sync);return()=>{window.removeEventListener("online",sync);window.removeEventListener("labaflow:offline-synced",sync)}},[]);
 useEffect(()=>{const timer=setInterval(()=>{if(navigator.onLine)Object.entries(qrPayments).forEach(([orderId,q])=>{if(q.status==="awaiting_payment")checkQr(orderId,false)})},3000);return()=>clearInterval(timer)},[qrPayments]);
 async function load(){setMessage("");if(!navigator.onLine){const cached=await getReference<{orders:Order[];access:Access;gcash:GCashAccount|null}>(CACHE);if(cached){setOrders(cached.orders);setAccess(cached.access);setGcash(cached.gcash);setMessage("Offline mode: showing the most recently synchronized orders.")}return}const [a,o,g]=await Promise.all([supabase.rpc("get_current_access_context"),supabase.from("laundry_orders").select("id,order_code,branch_id,customer_id,status,payment_status,subtotal,discount,total,amount_paid,created_at,delivery_addon_type,delivery_distance_label,delivery_fee,delivery_jobs(job_type,status),customers(full_name,customer_code)").order("created_at",{ascending:false}).limit(200),supabase.rpc("get_organization_payment_account",{p_payment_method:"gcash"})]);if(a.error||o.error){setMessage(a.error?.message||o.error?.message||"Unable to load orders");return}const normalized=(o.data??[]).map((x:any)=>({...x,subtotal:Number(x.subtotal),discount:Number(x.discount),total:Number(x.total),amount_paid:Number(x.amount_paid),delivery_fee:Number(x.delivery_fee??0)}));const nextAccess=a.data??{},nextGcash=!g.error?(g.data??null):gcash;setAccess(nextAccess);setOrders(normalized);setGcash(nextGcash);await cacheReference(CACHE,{orders:normalized,access:nextAccess,gcash:nextGcash})}
 const permissions=access.permissions??{};const workflow=(access.settings?.order_workflow?.stages as string[]|undefined)?.filter(Boolean)??fallbackWorkflow;const methods=(access.settings?.payment_methods?.methods as string[]|undefined)?.filter(Boolean)??fallbackPayments;const requireFull=Boolean(access.settings?.completion_rules?.require_full_payment_before_completion);const nextStatus=(s:string)=>{const i=workflow.indexOf(s);return i>=0&&i<workflow.length-1?workflow[i+1]:null};
 async function move(o:Order){const next=nextStatus(o.status);if(!next)return;if(next==="completed"&&requireFull&&o.amount_paid<o.total){setMessage(`Full payment is required before completing ${o.order_code}.`);return}if(!navigator.onLine){await queueJob("update_order_status",{p_order_id:o.id,p_status:next,p_notes:"Queued while offline"});setOrders(x=>x.map(z=>z.id===o.id?{...z,status:next}:z));setMessage(`${o.order_code} moved to ${label(next)} offline. Pending sync.`);return}const {error}=await supabase.rpc("update_order_status",{p_order_id:o.id,p_status:next,p_notes:null});if(error)setMessage(error.message);else await load()}
 async function pay(o:Order){const p=payment[o.id]??{amount:"",method:methods[0]??"cash",reference:""};if(p.method==="qrph"){if(!navigator.onLine){setMessage("QR Ph requires an internet connection.");return}await generateQr(o);return}const amount=Number(p.amount);if(!amount)return;if(!navigator.onLine){if(p.method!=="cash"){setMessage(`${label(p.method)} payment requires an internet connection. Cash can be recorded offline.`);return}if(amount>Math.max(o.total-o.amount_paid,0)){setMessage("Payment exceeds remaining balance.");return}await queueJob("record_cash_payment",{p_order_id:o.id,p_amount:amount,p_reference:p.reference||null});setOrders(x=>x.map(z=>z.id===o.id?{...z,amount_paid:z.amount_paid+amount,payment_status:z.amount_paid+amount>=z.total?"paid":"partial"}:z));setPayment(x=>({...x,[o.id]:{amount:"",method:"cash",reference:""}}));setMessage(`Cash payment ${peso.format(amount)} saved offline. Pending sync.`);return}const {error}=await supabase.rpc("record_order_payment",{p_order_id:o.id,p_amount:amount,p_method:p.method,p_reference:p.reference||null});if(error){setMessage(error.message);return}setPayment(x=>({...x,[o.id]:{amount:"",method:p.method,reference:""}}));await load();setMessage(amount>=Math.max(o.total-o.amount_paid,0)?"Payment completed. Loyalty rewards were calculated automatically.":"Partial payment recorded.")}
 async function generateQr(o:Order){const p=payment[o.id]??{amount:"",method:"qrph",reference:""};const balance=Math.max(o.total-o.amount_paid,0),amount=Number(p.amount)||balance;if(amount<=0||amount>balance){setMessage("Enter a valid QR Ph payment amount up to the remaining balance.");return}setQrPayments(x=>({...x,[o.id]:{amount,status:"creating",loading:true,error:null}}));const {data,error}=await supabase.functions.invoke("create-paymongo-qr",{body:{order_id:o.id,amount}});if(error||data?.error){const detail=data?.error||error?.message||"Unable to generate QR Ph";setQrPayments(x=>({...x,[o.id]:{amount,status:"failed",loading:false,error:detail}}));setMessage(detail);return}setQrPayments(x=>({...x,[o.id]:{transaction_id:data.transaction_id,payment_intent_id:data.payment_intent_id,amount:Number(data.amount),status:data.status,qr_image_data:data.qr_image_data,expires_at:data.expires_at,test_mode:Boolean(data.test_mode),test_url:data.test_url??null,loading:false,error:null}}));setMessage(data.test_mode?`Test QR Ph generated for ${o.order_code}. Use Simulate Test Payment — do not scan the test QR.`:`QR Ph generated for ${o.order_code}. Waiting for PayMongo payment confirmation.`)}
 async function checkQr(orderId:string,showMessage=true){if(!navigator.onLine)return;const {data,error}=await supabase.rpc("get_order_gateway_transaction",{p_order_id:orderId});if(error){if(showMessage)setMessage(error.message);return}if(!data)return;const status=data.status as string;setQrPayments(x=>({...x,[orderId]:{...(x[orderId]??{amount:Number(data.amount)}),transaction_id:data.id,payment_intent_id:data.provider_payment_intent_id,amount:Number(data.amount),status,qr_image_data:data.qr_image_data,expires_at:data.expires_at,loading:false,error:null}}));if(status==="paid"){await load();setMessage("QR Ph payment confirmed automatically. Order payment and loyalty were updated.")}else if(showMessage)setMessage(status==="expired"?"QR Ph expired. Generate a new QR to continue.":status==="failed"?"QR Ph payment failed. Generate a new QR to retry.":"Payment is still awaiting confirmation.")}
 const visible=useMemo(()=>{const q=search.trim().toLowerCase();return orders.filter(o=>(statusFilter==="all"||o.status===statusFilter)&&(!q||`${o.order_code} ${o.customers?.full_name??""} ${o.customers?.customer_code??""}`.toLowerCase().includes(q)))},[orders,search,statusFilter]);const stats=useMemo(()=>({open:orders.filter(o=>!["completed","cancelled"].includes(o.status)).length,ready:orders.filter(o=>o.status==="ready").length,unpaid:orders.filter(o=>o.amount_paid<o.total).length,revenue:orders.filter(o=>o.payment_status==="paid"||o.amount_paid>=o.total).reduce((n,o)=>n+o.total,0)}),[orders]);
 return <main className="workspace ordersExperience"><header><div><p className="eyebrow">ORDERS</p><h1>Order Management</h1><p className="muted">Track laundry progress and payments. Cached orders, workflow updates, and cash payments remain available during temporary internet loss.</p></div><div className="headerActions"><button className="primary" onClick={()=>location.href="/new-order"}>+ New Order</button></div></header>{message&&<p className="notice">{message}</p>}<section className="stats orderStats"><article><span>Open Orders</span><strong>{stats.open}</strong></article><article><span>Ready</span><strong>{stats.ready}</strong></article><article><span>Unpaid</span><strong>{stats.unpaid}</strong></article><article><span>Paid Revenue</span><strong>{peso.format(stats.revenue)}</strong></article></section><section className="panel ordersPanel"><div className="ordersToolbar"><div><h2>Orders</h2><span>{visible.length} shown · {orders.length} total</span></div><div className="ordersFilters"><select value={statusFilter} onChange={e=>setStatusFilter(e.target.value)}><option value="all">All statuses</option>{workflow.map(s=><option key={s} value={s}>{label(s)}</option>)}</select><input placeholder="Search order or customer" value={search} onChange={e=>setSearch(e.target.value)}/></div></div><div className="orderList">{visible.map(o=>{const p=payment[o.id]??{amount:"",method:methods[0]??"cash",reference:""};const next=nextStatus(o.status),paid=o.payment_status==="paid"||o.amount_paid>=o.total,balance=Math.max(o.total-o.amount_paid,0);return <article className="orderCard polishedOrder" key={o.id}><div className="orderTop"><div><strong>{o.order_code}</strong><small>{o.customers?.full_name??"Walk-in"}{o.customers?.customer_code?` · ${o.customers.customer_code}`:""}</small></div><div><span className={`status ${o.status}`}>{label(o.status)}</span><b>{peso.format(o.total)}</b></div></div><div className="orderMeta"><span>Paid <b>{peso.format(o.amount_paid)}</b></span><span>Balance <b>{peso.format(balance)}</b></span><span>Payment <b>{paid?"Paid":label(o.payment_status)}</b></span></div><div className="orderActions">{permissions.process_orders&&next&&<button className="secondary" disabled={next==="completed"&&requireFull&&!paid} onClick={()=>move(o)}>{next==="completed"?"Complete Order":`Move to ${label(next)}`}</button>}{permissions.record_payments&&(paid?<span className="status active">✓ Paid</span>:<><select value={p.method} onChange={e=>setPayment(x=>({...x,[o.id]:{...p,method:e.target.value}}))}>{methods.map(m=><option key={m} value={m}>{label(m)}</option>)}</select><input type="number" min="0.01" max={balance} step="0.01" placeholder={`Balance ${peso.format(balance)}`} value={p.amount} onChange={e=>setPayment(x=>({...x,[o.id]:{...p,amount:e.target.value}}))}/><input placeholder="Reference (optional)" value={p.reference} onChange={e=>setPayment(x=>({...x,[o.id]:{...p,reference:e.target.value}}))}/><button className="primary" onClick={()=>pay(o)}>{p.method==="qrph"?"Generate QR Ph":p.method==="gcash"?"Confirm GCash Payment":"Record Payment"}</button></>)}</div></article>})}{!visible.length&&<p className="empty">No orders match your filters.</p>}</div></section></main>
}
