"use client";
import {useEffect,useMemo,useState} from "react";import {supabase} from "../../lib/supabase";import {cacheReference,getReference,queueJob} from "../../lib/offlineQueue";import {currentOfflineScope} from "../../lib/offlineScope";
type DispatchJob={job_type:"pickup"|"delivery";status:string};type Order={id:string;order_code:string;branch_id:string;customer_id:string|null;status:string;payment_status:string;subtotal:number;discount:number;total:number;amount_paid:number;created_at:string;delivery_addon_type?:"pickup"|"delivery"|"pickup_delivery"|null;delivery_distance_label?:string|null;delivery_fee?:number;delivery_jobs?:DispatchJob[];customers:{full_name:string;customer_code:string}|null};type Access={settings?:Record<string,any>;permissions?:Record<string,boolean>};type GCashAccount={integration_type:string;merchant_name:string|null;account_number:string|null;qr_image_url:string|null;instructions:string|null;active:boolean};type QrPayment={amount:number;status:string;loading?:boolean;error?:string|null};const peso=new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP"}),label=(s:string)=>s==="qrph"?"QR Ph":s.replaceAll("_"," ").replace(/\b\w/g,c=>c.toUpperCase()),fallbackWorkflow=["received","sorting","washing","drying","folding","ready","completed"],fallbackPayments=["cash","gcash","maya","card","bank_transfer","qrph"],CACHE="orders-page-v1";
export default function OrdersPage(){const [orders,setOrders]=useState<Order[]>([]),[access,setAccess]=useState<Access>({}),[gcash,setGcash]=useState<GCashAccount|null>(null),[message,setMessage]=useState(""),[search,setSearch]=useState(""),[statusFilter,setStatusFilter]=useState("all"),[payment,setPayment]=useState<Record<string,{amount:string;method:string;reference:string}>>({}),[qrPayments,setQrPayments]=useState<Record<string,QrPayment>>({});useEffect(()=>{load();const sync=()=>setTimeout(()=>load(),500);window.addEventListener("online",sync);window.addEventListener("labaflow:offline-synced",sync);return()=>{window.removeEventListener("online",sync);window.removeEventListener("labaflow:offline-synced",sync)}},[]);async function load(){setMessage("");const scope=await currentOfflineScope();if(!scope)return setMessage("Unable to identify the current offline workspace.");if(!navigator.onLine){const cached=await getReference<{orders:Order[];access:Access;gcash:GCashAccount|null}>(CACHE,scope);if(cached){setOrders(cached.orders);setAccess(cached.access);setGcash(cached.gcash);setMessage("Offline mode: showing this organization's most recently synchronized orders.")}else setMessage("No offline Orders cache is available for this organization yet. Connect once to prepare offline processing.");return}const [a,o,g]=await Promise.all([supabase.rpc("get_current_access_context"),supabase.from("laundry_orders").select("id,order_code,branch_id,customer_id,status,payment_status,subtotal,discount,total,amount_paid,created_at,delivery_addon_type,delivery_distance_label,delivery_fee,delivery_jobs(job_type,status),customers(full_name,customer_code)").order("created_at",{ascending:false}).limit(200),supabase.rpc("get_organization_payment_account",{p_payment_method:"gcash"})]);if(a.error||o.error)return setMessage(a.error?.message||o.error?.message||"Unable to load orders");const normalized=(o.data??[]).map((x:any)=>({...x,subtotal:Number(x.subtotal),discount:Number(x.discount),total:Number(x.total),amount_paid:Number(x.amount_paid),delivery_fee:Number(x.delivery_fee??0)})),nextAccess=a.data??{},nextGcash=!g.error?(g.data??null):gcash;setAccess(nextAccess);setOrders(normalized);setGcash(nextGcash);await cacheReference(CACHE,{orders:normalized,access:nextAccess,gcash:nextGcash},scope)}const permissions=access.permissions??{},workflow=(access.settings?.order_workflow?.stages as string[]|undefined)?.filter(Boolean)??fallbackWorkflow,methods=(access.settings?.payment_methods?.methods as string[]|undefined)?.filter(Boolean)??fallbackPayments,requireFull=Boolean(access.settings?.completion_rules?.require_full_payment_before_completion),nextStatus=(s:string)=>{const i=workflow.indexOf(s);return i>=0&&i<workflow.length-1?workflow[i+1]:null};async function move(o:Order){const next=nextStatus(o.status);if(!next)return;if(next==="completed"&&requireFull&&o.amount_paid<o.total)return setMessage(`Full payment is required before completing ${o.order_code}.`);if(!navigator.onLine){const scope=await currentOfflineScope();if(!scope)return setMessage("Unable to identify the current organization.");await queueJob("update_order_status",{p_order_id:o.id,p_status:next,p_notes:"Queued while offline"},{scope});setOrders(x=>x.map(z=>z.id===o.id?{...z,status:next}:z));setMessage(`${o.order_code} moved to ${label(next)} offline. Pending sync.`);return}const {error}=await supabase.rpc("update_order_status",{p_order_id:o.id,p_status:next,p_notes:null});if(error)setMessage(error.message);else await load()}async function pay(o:Order){const p=payment[o.id]??{amount:"",method:methods[0]??"cash",reference:""};if(p.method==="qrph"){if(!navigator.onLine)return setMessage("QR Ph requires an internet connection.");return setMessage("QR Ph remains online-only while offline resilience is enabled.")}const amount=Number(p.amount);if(!amount)return;if(!navigator.onLine){if(p.method!=="cash")return setMessage(`${label(p.method)} payment requires an internet connection. Cash can be recorded offline.`);if(amount>Math.max(o.total-o.amount_paid,0))return setMessage("Payment exceeds remaining balance.");const scope=await currentOfflineScope();if(!scope)return setMessage("Unable to identify the current organization.");await queueJob("record_cash_payment",{p_order_id:o.id,p_amount:amount,p_reference:p.reference||null},{scope});setOrders(x=>x.map(z=>z.id===o.id?{...z,amount_paid:z.amount_paid+amount,payment_status:z.amount_paid+amount>=z.total?"paid":"partial"}:z));setPayment(x=>({...x,[o.id]:{amount:"",method:"cash",reference:""}}));setMessage(`Cash payment ${peso.format(amount)} saved offline. Pending sync.`);return}const {error}=await supabase.rpc("record_order_payment",{p_order_id:o.id,p_amount:amount,p_method:p.method,p_reference:p.reference||null});if(error)return setMessage(error.message);setPayment(x=>({...x,[o.id]:{amount:"",method:p.method,reference:""}}));await load();setMessage(amount>=Math.max(o.total-o.amount_paid,0)?"Payment completed. Loyalty rewards were calculated automatically.":"Partial payment recorded.")}const visible=useMemo(()=>{const q=search.trim().toLowerCase();return orders.filter(o=>(statusFilter==="all"||o.status===statusFilter)&&(!q||`${o.order_code} ${o.customers?.full_name??""} ${o.customers?.customer_code??""}`.toLowerCase().includes(q)))},[orders,search,statusFilter]),stats=useMemo(()=>({open:orders.filter(o=>!["completed","cancelled"].includes(o.status)).length,ready:orders.filter(o=>o.status==="ready").length,unpaid:orders.filter(o=>o.amount_paid<o.total).length,revenue:orders.filter(o=>o.payment_status==="paid"||o.amount_paid>=o.total).reduce((n,o)=>n+o.total,0)}),[orders]);return <main className="workspace ordersExperience"><header><div><p className="eyebrow">ORDERS</p><h1>Order Management</h1><p className="muted">Cached orders, workflow updates and cash payments are isolated to the signed-in organization.</p></div><div className="headerActions"><button className="primary" onClick={()=>location.href="/new-order"}>+ New Order</button></div></header>{message&&<p className="notice">{message}</p>}<section className="stats orderStats"><article><span>Open Orders</span><strong>{stats.open}</strong></article><article><span>Ready</span><strong>{stats.ready}</strong></article><article><span>Unpaid</span><strong>{stats.unpaid}</strong></article><article><span>Paid Revenue</span><strong>{peso.format(stats.revenue)}</strong></article></section><section className="panel ordersPanel"><div className="ordersToolbar"><div><h2>Orders</h2><span>{visible.length} shown · {orders.length} total</span></div><div className="ordersFilters"><select value={statusFilter} onChange={e=>setStatusFilter(e.target.value)}><option value="all">All statuses</option>{workflow.map(s=><option key={s} value={s}>{label(s)}</option>)}</select><input placeholder="Search order or customer" value={search} onChange={e=>setSearch(e.target.value)}/></div></div><div className="orderList">{visible.map(o=>{const p=payment[o.id]??{amount:"",method:methods[0]??"cash",reference:""},next=nextStatus(o.status),paid=o.payment_status==="paid"||o.amount_paid>=o.total,balance=Math.max(o.total-o.amount_paid,0);return <article className="orderCard polishedOrder" key={o.id}><div className="orderTop"><div><strong>{o.order_code}</strong><small>{o.customers?.full_name??"Walk-in"}{o.customers?.customer_code?` · ${o.customers.customer_code}`:""}</small></div><div><span className={`status ${o.status}`}>{label(o.status)}</span><b>{peso.format(o.total)}</b></div></div><div className="orderMeta"><span>Paid <b>{peso.format(o.amount_paid)}</b></span><span>Balance <b>{peso.format(balance)}</b></span><span>Payment <b>{paid?"Paid":label(o.payment_status)}</b></span></div><div className="orderActions">{permissions.process_orders&&next&&<button className="secondary" disabled={next==="completed"&&requireFull&&!paid} onClick={()=>move(o)}>{next==="completed"?"Complete Order":`Move to ${label(next)}`}</button>}{permissions.record_payments&&(paid?<span className="status active">✓ Paid</span>:<><select value={p.method} onChange={e=>setPayment(x=>({...x,[o.id]:{...p,method:e.target.value}}))}>{methods.map(m=><option key={m} value={m}>{label(m)}</option>)}</select><input type="number" min="0.01" max={balance} step="0.01" placeholder={`Balance ${peso.format(balance)}`} value={p.amount} onChange={e=>setPayment(x=>({...x,[o.id]:{...p,amount:e.target.value}}))}/><input placeholder="Reference (optional)" value={p.reference} onChange={e=>setPayment(x=>({...x,[o.id]:{...p,reference:e.target.value}}))}/><button className="primary" onClick={()=>pay(o)}>{p.method==="qrph"?"Generate QR Ph":p.method==="gcash"?"Confirm GCash Payment":"Record Payment"}</button></>)}</div></article>})}{!visible.length&&<p className="empty">No orders match your filters.</p>}</div></section></main>}
