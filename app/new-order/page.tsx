"use client";

import {FormEvent,useEffect,useMemo,useState} from "react";
import type {Session} from "@supabase/supabase-js";
import QRCode from "qrcode";
import {supabase} from "../../lib/supabase";
import {queueJob,cacheReference,getReference} from "../../lib/offlineQueue";

type Branch={id:string;name:string}; type Customer={id:string;full_name:string;customer_code:string;loyalty_points:number;lifetime_points:number;lifetime_visits:number;lifetime_spend:number}; type Level={id:string;name:string;minimum_points:number}; type Service={id:string;name:string;pricing_unit:string;default_price:number;loyalty_points:number}; type AddonType="pickup"|"delivery"|"pickup_delivery"; type Rate={id:string;addon_type:AddonType;label:string;min_km:number;max_km:number|null;price:number}; type CartItem={service_id:string;quantity:number}; type AppliedPromo={id:string;code:string;name:string|null;discount_type:"amount"|"percentage";discount_value:number;discount_amount:number;usage_count:number;usage_limit:number|null;starts_at:string|null;expires_at:string|null}; type DeliveryQuote={address_id:string;address_label:string;address_line:string;distance_km:number;rate_id:string;rate_label:string;price:number;addon_type:AddonType}; type CustomerContext={addressLabel:string;addressLine:string;distanceKm:number|null};
const peso=new Intl.NumberFormat("en-PH",{style:"currency",currency:"PHP"}); const addonLabels:Record<AddonType,string>={pickup:"Pickup",delivery:"Delivery",pickup_delivery:"Pickup + Delivery"};
export default function NewOrderPage(){
 const [session,setSession]=useState<Session|null>(null),[checking,setChecking]=useState(true),[message,setMessage]=useState(""); const [branches,setBranches]=useState<Branch[]>([]),[customers,setCustomers]=useState<Customer[]>([]),[levels,setLevels]=useState<Level[]>([]),[services,setServices]=useState<Service[]>([]),[rates,setRates]=useState<Rate[]>([]); const [branchId,setBranchId]=useState(""),[customerId,setCustomerId]=useState(""),[notes,setNotes]=useState(""),[loyaltyPoints,setLoyaltyPoints]=useState("0"),[cart,setCart]=useState<CartItem[]>([]); const [promoInput,setPromoInput]=useState(""),[appliedPromo,setAppliedPromo]=useState<AppliedPromo|null>(null),[promoChecking,setPromoChecking]=useState(false); const [addon,setAddon]=useState<{type:AddonType;rate:Rate;quote?:DeliveryQuote}|null>(null),[quoteChecking,setQuoteChecking]=useState<AddonType|null>(null); const [customerContext,setCustomerContext]=useState<CustomerContext|null>(null),[qrImage,setQrImage]=useState<string|null>(null);
 useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);setChecking(false)});const {data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]); useEffect(()=>{if(session)load()},[session]); useEffect(()=>{if(customerId&&branchId&&navigator.onLine)loadCustomerContext();else setCustomerContext(null)},[customerId,branchId]); useEffect(()=>{const f=(e:Event)=>{const token=(e as CustomEvent<{token:string}>).detail?.token;if(token)selectCustomerFromQr(token)};window.addEventListener("labaflow:qr-scanned",f);return()=>window.removeEventListener("labaflow:qr-scanned",f)},[]);
 async function applyCached(x:any){if(!x)return;setBranches(x.branches??[]);setBranchId(b=>b||(x.branches?.[0]?.id??""));setCustomers(x.customers??[]);setLevels(x.levels??[]);setServices(x.services??[]);setRates(x.rates??[])}
 async function load(){if(!navigator.onLine){await applyCached(await getReference<any>("new-order-reference"));setMessage("Offline mode: using saved customers, services and pricing. New orders will be queued for sync.");return}const [a,c,l,s,r]=await Promise.all([supabase.rpc("get_current_access_context"),supabase.from("customers").select("id,full_name,customer_code,loyalty_points,lifetime_points,lifetime_visits,lifetime_spend").eq("active",true).order("full_name"),supabase.from("loyalty_levels").select("id,name,minimum_points").eq("active",true).order("minimum_points"),supabase.from("services").select("id,name,pricing_unit,default_price,loyalty_points").eq("active",true).order("name"),supabase.rpc("get_delivery_distance_rates",{p_addon_type:null})]);if(a.error||c.error||l.error||s.error||r.error){setMessage(a.error?.message||c.error?.message||l.error?.message||s.error?.message||r.error?.message||"");return}const data={branches:(a.data?.branches??[]) as Branch[],customers:(c.data??[]).map((x:any)=>({...x,loyalty_points:Number(x.loyalty_points??0),lifetime_points:Number(x.lifetime_points??0),lifetime_visits:Number(x.lifetime_visits??0),lifetime_spend:Number(x.lifetime_spend??0)})),levels:(l.data??[]).map((x:any)=>({...x,minimum_points:Number(x.minimum_points??0)})),services:(s.data??[]).map((x:any)=>({...x,default_price:Number(x.default_price),loyalty_points:Number(x.loyalty_points??0)})),rates:(r.data??[]).map((x:any)=>({...x,min_km:Number(x.min_km),max_km:x.max_km==null?null:Number(x.max_km),price:Number(x.price)}))};await applyCached(data);await cacheReference("new-order-reference",data)}
 async function loadCustomerContext(){const {data:a}=await supabase.from("customer_addresses").select("id,label,address_line,is_default").eq("customer_id",customerId).eq("active",true).order("is_default",{ascending:false}).limit(1);if(!a?.length){setCustomerContext(null);return}const x=a[0],{data:d}=await supabase.from("customer_address_branch_distances").select("distance_km").eq("address_id",x.id).eq("branch_id",branchId).maybeSingle();setCustomerContext({addressLabel:x.label,addressLine:x.address_line,distanceKm:d?Number(d.distance_km):null})}
 function levelFor(p:number){return [...levels].filter(l=>l.minimum_points<=p).sort((a,b)=>b.minimum_points-a.minimum_points)[0]?.name??"Member"} function addService(s:Service){setCart(x=>{const f=x.find(i=>i.service_id===s.id);return f?x.map(i=>i.service_id===s.id?{...i,quantity:i.quantity+1}:i):[...x,{service_id:s.id,quantity:1}]});setAppliedPromo(null)} const serviceSubtotal=useMemo(()=>cart.reduce((n,i)=>n+(services.find(s=>s.id===i.service_id)?.default_price??0)*i.quantity,0),[cart,services]); const deliveryFee=addon?.rate.price??0,grossAmount=Math.max(serviceSubtotal+deliveryFee,0),promoDiscount=Math.min(appliedPromo?.discount_amount??0,grossAmount),selectedCustomer=customers.find(c=>c.id===customerId)??null,beforeLoyalty=Math.max(grossAmount-promoDiscount,0),maxRedeem=Math.min(selectedCustomer?.loyalty_points??0,Math.floor(beforeLoyalty)),redeem=Math.min(Math.max(Math.floor(Number(loyaltyPoints)||0),0),maxRedeem),total=Math.max(beforeLoyalty-redeem,0);
 async function selectCustomerFromQr(token:string){if(!navigator.onLine){setMessage("QR customer lookup requires internet for now. Select a cached customer from the list.");return}const {data,error}=await supabase.rpc("lookup_customer_by_qr",{p_token:token});if(error)return setMessage(error.message);setCustomerId(data.id)} function scanWithCamera(){window.dispatchEvent(new Event("labaflow:open-camera-qr"))}
 async function applyPromo(){if(!navigator.onLine){setMessage("Promo validation requires internet. You can create a standard offline order without a promo.");return}const code=promoInput.trim().toUpperCase();if(!code)return setAppliedPromo(null);setPromoChecking(true);const {data,error}=await supabase.rpc("validate_promo_code",{p_code:code,p_order_amount:grossAmount});setPromoChecking(false);if(error)return setMessage(error.message);setAppliedPromo({...data,discount_value:Number(data.discount_value),discount_amount:Number(data.discount_amount)} as AppliedPromo)}
 async function chooseAddon(type:AddonType){if(!navigator.onLine){setMessage("Pickup/Delivery quoting requires internet for now.");return}if(!customerId)return setMessage("Select a registered customer first.");setQuoteChecking(type);const {data,error}=await supabase.rpc("get_customer_delivery_quote",{p_customer_id:customerId,p_branch_id:branchId,p_addon_type:type});setQuoteChecking(null);if(error)return setMessage(error.message);const q={...data,distance_km:Number(data.distance_km),price:Number(data.price)} as DeliveryQuote;const rate=rates.find(r=>r.id===q.rate_id)!;setAddon({type,rate,quote:q})}
 async function showCustomerQr(){if(!selectedCustomer||!navigator.onLine)return setMessage("Customer QR retrieval requires internet for now.");const {data,error}=await supabase.rpc("get_customer_qr",{p_customer_id:selectedCustomer.id});if(error)return setMessage(error.message);setQrImage(await QRCode.toDataURL(`labaflow:${data.qr_token}`,{width:360,margin:2}))}
 function clear(){setCart([]);setPromoInput("");setAppliedPromo(null);setLoyaltyPoints("0");setNotes("");setAddon(null)}
 async function createOrder(e:FormEvent){e.preventDefault();if(!cart.length)return setMessage("Add at least one laundry service.");const payload={p_branch_id:branchId,p_customer_id:customerId||null,p_items:cart,p_discount:0,p_notes:notes||null,p_due_at:null,p_delivery_addon_type:addon?.type??null,p_delivery_distance_rate_id:addon?.rate.id??null,p_loyalty_points_to_redeem:redeem,p_promo_code:appliedPromo?.code??null};if(!navigator.onLine){await queueJob("create_order",payload);clear();setMessage("Order saved safely on this device. It will sync automatically when internet returns.");return}const {data,error}=await supabase.rpc("create_laundry_order",payload);if(error){setMessage(error.message);return}setMessage(`Order ${data.order_code} created.`);clear();await load()}
 if(checking)return <main className="center"><div className="loader">Loading order form…</div></main>;if(!session)return <main className="center"><section className="onboardCard"><h2>Sign in required</h2></section></main>;
 return <main className="workspace newOrderExperience"><header className="newOrderHeader"><div><p className="eyebrow">NEW ORDER</p><h1>New Order</h1><p className="muted">Create a new laundry order for your customer.</p></div></header>{message&&<p className="notice">{message}</p>}<form onSubmit={createOrder}><section className="newOrderMain"><article className="panel orderDetailsPanel"><div className="sectionTitle"><div className="sectionIcon">▤</div><div><h2>Order Details</h2><p>Customer and order information</p></div></div><div className="gridForm"><label>Customer<select value={customerId} onChange={e=>setCustomerId(e.target.value)}><option value="">Walk-in / No customer</option>{customers.map(c=><option key={c.id} value={c.id}>{c.full_name} — {c.customer_code}</option>)}</select></label><label>Branch<select value={branchId} onChange={e=>setBranchId(e.target.value)} required>{branches.map(b=><option key={b.id} value={b.id}>{b.name}</option>)}</select></label></div>{selectedCustomer&&<div className="notice"><strong>{selectedCustomer.full_name}</strong> · {levelFor(selectedCustomer.lifetime_points)} · {selectedCustomer.loyalty_points} available points</div>}<label className="notesLabel">Notes <small>(Optional)</small><textarea value={notes} onChange={e=>setNotes(e.target.value)}/></label><h3>Laundry Services</h3><div className="servicePicker">{services.map(s=><button type="button" key={s.id} onClick={()=>addService(s)}><strong>{s.name}</strong><small>{peso.format(s.default_price)} / {s.pricing_unit}</small></button>)}</div><h3>Order Add-ons</h3><div className="servicePicker">{(["pickup","delivery","pickup_delivery"] as AddonType[]).map(x=><button type="button" key={x} onClick={()=>chooseAddon(x)} disabled={quoteChecking!==null}><strong>{addonLabels[x]}</strong><small>Online quote</small></button>)}</div>{addon&&<div className="notice">{addonLabels[addon.type]} · {peso.format(addon.rate.price)} <button type="button" className="miniBtn" onClick={()=>setAddon(null)}>Remove</button></div>}</article><aside className="newOrderSide"><section className="panel quickActions"><h2>Quick Actions</h2><button type="button" onClick={scanWithCamera}><span>◉</span><div><strong>Scan with Camera</strong><small>Scan customer QR code</small></div></button>{selectedCustomer&&<button type="button" onClick={showCustomerQr}><span>▦</span><div><strong>View Customer QR</strong></div></button>}</section><section className="panel orderSummary"><h2>Order Summary</h2>{cart.length?cart.map(i=>{const s=services.find(x=>x.id===i.service_id)!;return <div className="cartRow" key={i.service_id}><div><strong>{s.name}</strong></div><input type="number" min="0.01" step="0.01" value={i.quantity} onChange={e=>setCart(x=>x.map(z=>z.service_id===i.service_id?{...z,quantity:Number(e.target.value)}:z))}/><b>{peso.format(s.default_price*i.quantity)}</b></div>}):<p className="empty">No services added yet.</p>}<div className="promoBox"><strong>Promo Code</strong><div className="promoApply"><input value={promoInput} onChange={e=>setPromoInput(e.target.value.toUpperCase())}/><button type="button" className="secondary" onClick={applyPromo} disabled={promoChecking}>Apply</button></div></div>{selectedCustomer&&<div className="loyaltyBox"><strong>Redeem Loyalty Points</strong><input type="number" min="0" max={maxRedeem} value={loyaltyPoints} onChange={e=>setLoyaltyPoints(e.target.value)}/></div>}<div className="totals"><div><span>Subtotal</span><b>{peso.format(serviceSubtotal)}</b></div>{addon&&<div><span>Pickup/Delivery</span><b>{peso.format(deliveryFee)}</b></div>}{appliedPromo&&<div><span>Promo</span><b>−{peso.format(promoDiscount)}</b></div>}{redeem>0&&<div><span>Loyalty</span><b>−{peso.format(redeem)}</b></div>}<div className="grand"><span>Total</span><b>{peso.format(total)}</b></div></div><button className="primary wide" disabled={!cart.length}>{navigator.onLine?"Create Laundry Order":"Save Offline Order"}</button></section></aside></section></form>{qrImage&&<div className="modalBackdrop"><section className="modal qrModal"><button className="iconBtn" onClick={()=>setQrImage(null)}>×</button><img src={qrImage} alt="Customer QR"/></section></div>}</main>
}
