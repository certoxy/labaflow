"use client";
import {useCallback,useEffect,useState} from "react";import {supabase} from "../lib/supabase";import {addHistory,jobs,removeJob,updateJob} from "../lib/offlineQueue";
export default function OfflineEnhancer(){const [online,setOnline]=useState(true),[pending,setPending]=useState(0),[syncing,setSyncing]=useState(false);const refresh=useCallback(async()=>{try{setPending((await jobs()).length)}catch{}},[]);const sync=useCallback(async()=>{if(!navigator.onLine||syncing)return;setSyncing(true);try{for(const job of await jobs()){await updateJob({...job,status:"syncing",error:undefined});let error:any=null;if(job.kind==="create_order")({error}=await supabase.rpc("sync_offline_order",{p_operation_id:job.id,...job.payload}));else if(job.kind==="update_order_status")({error}=await supabase.rpc("sync_offline_order_status",{p_operation_id:job.id,...job.payload}));else if(job.kind==="record_cash_payment")({error}=await supabase.rpc("sync_offline_cash_payment",{p_operation_id:job.id,...job.payload}));else if(job.kind==="create_customer")({error}=await supabase.rpc("sync_offline_customer",{p_operation_id:job.id,...job.payload}));if(error){await updateJob({...job,status:"failed",error:error.message});continue}await addHistory({id:job.id,kind:job.kind,syncedAt:new Date().toISOString()});await removeJob(job.id);window.dispatchEvent(new CustomEvent("labaflow:offline-synced",{detail:{operationId:job.id,kind:job.kind}}))}}finally{setSyncing(false);await refresh()}},[refresh,syncing]);useEffect(()=>{setOnline(navigator.onLine);refresh();const on=()=>{setOnline(true);setTimeout(()=>sync(),300)},off=()=>setOnline(false),changed=()=>refresh();window.addEventListener("online",on);window.addEventListener("offline",off);window.addEventListener("labaflow:queue-changed",changed);if("serviceWorker" in navigator)navigator.serviceWorker.register("/sw.js").catch(console.error);if(navigator.onLine)setTimeout(()=>sync(),800);return()=>{window.removeEventListener("online",on);window.removeEventListener("offline",off);window.removeEventListener("labaflow:queue-changed",changed)}},[refresh,sync]);const text=!online?`● Offline${pending?` · ${pending} pending sync`:""}`:syncing?`↻ Syncing ${pending} change${pending===1?"":"s"}…`:pending?`● Online · ${pending} pending sync`:"● Online";return <div aria-live="polite" onClick={()=>location.href="/sync"} title="Open Sync Center" style={{cursor:"pointer",position:"fixed",right:16,bottom:16,zIndex:9999,padding:"8px 12px",borderRadius:999,fontSize:13,fontWeight:700,boxShadow:"0 4px 18px rgba(0,0,0,.15)",background:online&&!pending?"#ecfdf5":"#fff7ed",color:online&&!pending?"#065f46":"#9a3412",border:`1px solid ${online&&!pending?"#a7f3d0":"#fed7aa"}`}}>{text}</div>}
