"use client";
import {useEffect} from "react";
export default function BranchCodeLockEnhancer(){
 useEffect(()=>{if(!location.pathname.startsWith("/organization"))return;let raf=0;const apply=()=>{cancelAnimationFrame(raf);raf=requestAnimationFrame(()=>{document.querySelectorAll<HTMLFormElement>(".inlineBranchEdit").forEach(form=>{const labels=Array.from(form.querySelectorAll("label"));const codeLabel=labels.find(l=>l.textContent?.trim().startsWith("Code"));const input=codeLabel?.querySelector<HTMLInputElement>("input");if(!input)return;input.readOnly=true;input.disabled=true;input.title="Branch code is permanent after creation because it is used in generated product SKUs.";if(!codeLabel?.querySelector(".branchCodeHint")){const hint=document.createElement("small");hint.className="branchCodeHint";hint.textContent="Permanent after creation · used for product SKUs";codeLabel?.appendChild(hint)}})})};apply();const observer=new MutationObserver(apply);observer.observe(document.body,{childList:true,subtree:true});return()=>{observer.disconnect();cancelAnimationFrame(raf)}},[]);return null}
