import SharedSidebar from "../../SharedSidebar";
export default function LocalStaffLayout({children}:{children:React.ReactNode}){return <div className="appShell"><SharedSidebar active="staff-access"/>{children}</div>}
